# Copy to .env and fill in. .env is gitignored.

# OpenRouter — Planner, Verifier, Refutation Agent, Generator, prose Fact Extractor.
OPENROUTER_API_KEY=

# SEC EDGAR User-Agent (required by SEC fair-use policy). Format: "AppName email@example.com".
EDGAR_USER_AGENT=cas.brag dwipert@example.com
